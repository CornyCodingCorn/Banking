<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:CornUI.Controls">

    <!--#region SysButton-->
    <Style TargetType="{x:Type Button}"
           x:Key="SysButton">
        <Style.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="..\/Asset/SystemIcon.xaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Style.Resources>
        <Setter Property="WindowChrome.CaptionHeight" Value="35"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Height" Value="19"/>
        <Setter Property="Width" Value="19"/>
        <Setter Property="Padding" Value="1,1,1,1"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Opacity" Value="0.75"/>
    </Style>
    <!--#endregion-->
    <!--#region Wnd-->
    <Style x:Name="window" TargetType="{x:Type local:Cwnd}"
           x:Key="CwndStyle">
        <Style.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <ResourceDictionary Source="..\/Asset/SystemIcon.xaml"/>
                    <ResourceDictionary Source="..\/Utility/Conversion.xaml"/>
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </Style.Resources>
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome CaptionHeight="35" ResizeBorderThickness="10"/>
            </Setter.Value>
        </Setter>
        <Setter Property="WindowState" Value="{Binding CwndWindowState}"/>
        <Setter Property="WindowStyle" Value="None"/>
        <Setter Property="ResizeMode" Value="NoResize"/>
        <Setter Property="AllowsTransparency" Value="True"/>
        <Setter Property="MinHeight" Value="200"/>
        <Setter Property="MinWidth" Value="300"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Cwnd}">
                    <Grid>
                        <!--#region WindowContent-->
                        <Border x:Name="windowBorder" 
                                Margin="{Binding BorderWidth, Converter={StaticResource ThicknessConverter}, FallbackValue=2}"
                                CornerRadius="{Binding BorderRoundEdgeRadius, FallbackValue=12}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="{Binding BorderWidth}" Opacity="0.5"/>
                            </Border.Effect>

                            <Grid>
                                <Rectangle x:Name="backGround"
                                           Fill="{Binding BackColor, FallbackValue=White}"
                                           RadiusX="{Binding RoundEdgeRadius, FallbackValue=10}"
                                           RadiusY="{Binding RoundEdgeRadius, FallbackValue=10}"/>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="{Binding TopBarHeight, FallbackValue=25}"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <Rectangle x:Name="topBar1"
                                               Grid.Row="0"
                                               Fill="{Binding TopBarColor, FallbackValue=LightGray}"
                                               RadiusX="{Binding RoundEdgeRadius, FallbackValue=10}"
                                               RadiusY="{Binding RoundEdgeRadius, FallbackValue=10}"/>
                                    <Rectangle x:Name="topBar2"
                                               Grid.Row="0"
                                               VerticalAlignment="Bottom"
                                               Height="{Binding RoundEdgeRadius, FallbackValue=10}"
                                               Fill="{Binding TopBarColor, FallbackValue=LightGray}"/>
                                    <Grid x:Name="topBarTitle"
                                          Grid.Row="0"
                                          VerticalAlignment="Stretch"
                                          HorizontalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Margin="10, 0, 10,0">
                                            <TextBlock VerticalAlignment="Center"
                                                       HorizontalAlignment="Center"
                                                       Text="{Binding CwndTitle, FallbackValue=GenericTitle}"
                                                       Foreground="{Binding CwndFontColor, FallbackValue=Black}"
                                                       FontSize="{Binding CwndFontSize, FallbackValue=14}"
                                                       FontFamily="{Binding CwndFontType, FallbackValue=Tahoma}"
                                                       FontWeight="{Binding CwndFontWeight, FallbackValue=Bold}"/>
                                        </Border>

                                        <Image Grid.Column="1" 
                                               Stretch="Uniform"
                                               Source="{Binding IconPath, FallbackValue=Icon.png}"
                                               Width="{Binding IconSize, FallbackValue=20}"
                                               VerticalAlignment="Stretch"
                                               HorizontalAlignment="Left"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Grid>

                                    <Grid HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          WindowChrome.IsHitTestVisibleInChrome="True"
                                          Width="82"
                                          Height="19">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Grid.Resources>
                                            <Style TargetType="Image">
                                                <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
                                                <Setter Property="Margin" Value="2,2,2,2"/>
                                            </Style>
                                        </Grid.Resources>

                                        <Image Grid.Column="2" Source="{StaticResource close}"/>
                                        <Image Grid.Column="1" Source="{StaticResource maximize}"/>
                                        <Image Grid.Column="0" Source="{StaticResource minimize}"/>

                                        <Button Grid.Column="2" x:Name="closeButton" Style="{StaticResource SysButton}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="20"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Grid.Column="1" x:Name="maximizeButton" Style="{StaticResource SysButton}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="20"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>

                                        <Button Grid.Column="0" x:Name="minimizeButton" Style="{StaticResource SysButton}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="20"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </Grid>

                                    <AdornerDecorator Grid.Row="1">
                                        <ContentPresenter/>
                                    </AdornerDecorator>
                                </Grid>
                            </Grid>
                        </Border>
                        <Border BorderThickness="1" 
                                BorderBrush="{Binding TopBarColor, FallbackValue=LightGray}" 
                                Margin="{Binding BorderWidth, Converter={StaticResource ThicknessConverter}, FallbackValue=2}"
                                Opacity="0.5"
                                CornerRadius="{Binding RoundEdgeRadius, FallbackValue=10}"/>
                        <!--#endregion-->
                        <!--#region Resize Grid-->
                        <Grid x:Name="resizeGrid" Margin="{Binding ResizeBorderWidth, Converter={StaticResource ThicknessConverter}, FallbackValue=2}">
                            <Grid.Resources>
                                <Style TargetType="Rectangle">
                                    <Setter Property="Fill" Value="Transparent"/>
                                    <Setter Property="Stroke" Value="Transparent"/>
                                </Style>
                            </Grid.Resources>

                            <Rectangle VerticalAlignment="Top"
                                       Height="{Binding ResizeRectSizeMiddle}"
                                       x:Name="top"
                                       Margin="10,0,10,0"
                                       Cursor="SizeNS"/>
                            <Rectangle x:Name="bottom"
                                       Height="{Binding ResizeRectSizeMiddle}"
                                       VerticalAlignment="Bottom"
                                       Margin="10,0,10,0"
                                       Cursor="SizeNS"/>
                            <Rectangle HorizontalAlignment="Left"
                                       Margin="0,5,0,5"
                                       Width="{Binding ResizeRectSizeMiddle}"
                                       x:Name="left"
                                       Cursor="SizeWE"/>
                            <Rectangle Margin="0,10,0,10"
                                       Width="{Binding ResizeRectSizeMiddle}"
                                       HorizontalAlignment="Right"
                                       x:Name="right"
                                       Cursor="SizeWE"/>
                            <Rectangle HorizontalAlignment="Left"
                                       VerticalAlignment="Bottom"
                                       Width="{Binding ResizeRectSizeCorner}"
                                       Height="{Binding ResizeRectSizeCorner}"
                                       x:Name="bottomLeft"
                                       Cursor="SizeNESW"/>
                            <Rectangle VerticalAlignment="Bottom"
                                       Height="{Binding ResizeRectSizeCorner}"
                                       Width="{Binding ResizeRectSizeCorner}"
                                       HorizontalAlignment="Right"
                                       x:Name="bottomRight"
                                       Cursor="SizeNWSE"/>
                            <Rectangle HorizontalAlignment="Right"
                                       Width="{Binding ResizeRectSizeCorner}"
                                       Height="{Binding ResizeRectSizeCorner}"
                                       VerticalAlignment="Top"
                                       x:Name="topRight"
                                       Cursor="SizeNESW"/>
                            <Rectangle HorizontalAlignment="Left"
                                       Width="{Binding ResizeRectSizeCorner}"
                                       VerticalAlignment="Top"
                                       Height="{Binding ResizeRectSizeCorner}"
                                       x:Name="topLeft"
                                       Cursor="SizeNWSE"/>
                        </Grid>
                        <!--#endregion-->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--#endregion-->
</ResourceDictionary>